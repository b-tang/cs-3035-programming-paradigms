n = do

-- smaller :: String -> String -> Bool

-- let s1 = readLn

-- let s2 = readLn

-- smaller s1 s2 | len1 /= len2    = (len1 < len2)

--               | otherwise = (s1 < s2)

--               where (len1, len2) = (length s1, length s2)